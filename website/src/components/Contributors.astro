---
import Icon from './ui/Icon.astro';

interface Props {
  contributors?: { initial: string; bg: string }[];
  label?: string;
}

const {
  contributors = [
    { initial: 'A', bg: '#3b82f6' },
    { initial: 'J', bg: '#10b981' },
    { initial: 'S', bg: '#f59e0b' },
    { initial: 'M', bg: '#8b5cf6' },
    { initial: 'K', bg: '#ef4444' },
  ],
  label = 'Trusted by contributors worldwide',
} = Astro.props;
---

<div class="flex items-center gap-3">
  <div class="flex -space-x-2">
    {contributors.map((c) => (
      <div
        class="flex h-8 w-8 items-center justify-center rounded-full border-2 border-background text-xs font-bold text-white"
        style={`background-color: ${c.bg};`}
      >
        {c.initial}
      </div>
    ))}
    <!-- Ellipsis avatar -->
    <div
      class="flex h-8 w-8 items-center justify-center rounded-full border-2 border-background bg-secondary text-muted-foreground"
      title="and many more"
      aria-label="and many more contributors"
    >
      <Icon name="moreHorizontal" size={14} />
    </div>
  </div>
  <span class="text-xs text-muted-foreground">{label}</span>
</div>
